#!/usr/bin/env bash
set -eo pipefail
if [[ -n $OMNIA_DEBUG ]]; then set -x; fi

(cd "$(cd "${0%/*/*}" && pwd)/lib"
	source ./log.sh

	# log calls to seth
	verbose --list "setzer call" "$@"

	exec setzer "$@"
)